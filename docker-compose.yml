version: "2"
services:
  mongo:
    container_name: thingYota-mongodb
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
  app:
    container_name: thingYta-api
    restart: always
    build: .
    ports:
      - "8000:8000"
    links:
      - mongo
